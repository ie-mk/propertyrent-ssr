import React from 'react';
import { Container } from '../../foundation';

const WhyUs = ({ text, img }) => (
  <Container>
    <img {...{ style: { display: 'block' } }} />
    <div></div>
  </Container>
);

export default WhyUs;
